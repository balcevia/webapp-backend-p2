1 step is run command docker-compose build
Make sure jar file webapp-assembly-1.0.jar is present inside project directory
2 step is run command docker-compose up
After that backend app is up and running on port 1070